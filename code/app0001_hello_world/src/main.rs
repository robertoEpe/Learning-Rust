// This is comment, comments are ignored by the computer

// All programs start at main
fn main() {
    // a macro that prints stuff to the standard output 
    println!("Hello, world, how are you?!");

    /* Other style of comments

        Everything inside "text" are called strings. They are text
    */
}
